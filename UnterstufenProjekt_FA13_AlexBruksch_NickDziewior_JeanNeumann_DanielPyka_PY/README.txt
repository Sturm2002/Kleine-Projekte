Empfohlene Python Version: 3.9.5

bei fehler : "pip3.9 install -r requirements.txt"

es sollte genügen die "start.bat" zu starten, ansonsten die main.py ausführen

die datenbank wird automatisch erstellt

viel glück !